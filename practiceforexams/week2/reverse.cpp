# include <iostream>
# include <algorithm> // this is necessary btw
using namespace std;

int main(){
    string a;
    // for (int i = 0; i<=)
    cin >> a;
    reverse(a.begin(), a.end()); // reverses the string
    cout << a<< endl;
    return 0;
}